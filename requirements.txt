scikit-learn>=1.5
numpy>=2.0

# That is the whole list.
#
# scikit-learn ships TfidfVectorizer, KMeans, LatentDirichletAllocation,
# LinearSVC, the 20 Newsgroups loader and every metric used here, so the
# original project's gensim, NLTK, spaCy, PyPDF2, python-docx, python-pptx,
# BeautifulSoup and Flask dependencies are all gone.
#
# Dropping gensim also removed its incompatibility with numpy 2.x on
# Python 3.12, and stop_words="english" replaced the NLTK stopword download.
